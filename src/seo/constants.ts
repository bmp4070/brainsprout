/**
 * Canonical site origin used to build absolute URLs (canonical links, OG
 * tags, sitemap entries). This is the ONE place to update once the real
 * production domain is known — everything else derives from it.
 */
export const SITE_URL = 'https://brainsprout.pages.dev';
